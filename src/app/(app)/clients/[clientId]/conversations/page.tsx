import { ComingSoon } from '@/components/coming-soon';

export default function ConversationsPage() {
  return (
    <ComingSoon
      title="Client Conversations"
      description="A full thread of every message MIRA sends to this client — initial outreach, reminders, replies, and outcomes. Today the latest send is shown on the client detail page."
    />
  );
}
