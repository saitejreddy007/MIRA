import { ComingSoon } from '@/components/coming-soon';

export default function OverridesPage() {
  return (
    <ComingSoon
      title="Client Overrides"
      description="Per-client voice, cadence, and segment overrides — useful for VIP clients who deserve a softer touch or slow payers who need firmer reminders. Per-invoice overrides are available today; full per-client controls arrive next."
    />
  );
}
